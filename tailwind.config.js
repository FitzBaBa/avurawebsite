/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    './index.html', // Adjust to include all HTML files
    './*.html',     // Add other HTML files if needed
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}